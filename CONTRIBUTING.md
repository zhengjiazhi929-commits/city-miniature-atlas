# Contributing

先运行杭州示范和[城市接入示例](docs/add-a-city.md)。如果要贡献新城市，请先完成一个有明确来源的最小城市范围，再逐步细化模型。

## 新城市提交应包含

- WGS84 边界（保留岛屿和洞）、展示中心、来源 URL、日期、许可与处理记录。
- 景点真实坐标、来源与显示范围；有独立模型才提供模型入口。
- 机场覆盖状态：有已核实市内机场则按原位置接入；市外机场仅说明位置/距离口径；未知不能假装没有。
- 默认远景与局部实际网页截图，注明当前是通用地图还是专门制作的微缩城市。
- 资源预算及退出场景后的释放检查。

## 改模型

固定树楼资产优先修改生成器，再重新生成并核对 City Kit 清单。墙面、玻璃、金属分槽，普通楼不用整实例染色。模型位置来自城市数据，不把杭州坐标当成其他城市的资产。

主路应留出建筑退距，树干、楼体与地基都需要接触实际地面。普通楼可概括数量和比例；地形、水系、道路连通与景点关系仍要真实。未知数据明确标注。

## 验证

运行 README 中的三个检查命令。数值通过后还需实际浏览器检查默认视角、点击、旋转、缩放、取消加载和离开释放。修改杭州 fabric 的依赖后须更新相应缓存，不能用旧缓存验证新代码。

## 提交范围

提交代码、必要运行数据、许可证与可复现说明。不要提交账号凭证、个人路径、聊天记录、浏览器配置、原始研究截图或未经许可再分发的参考文件。大数据先说明必要性与来源，避免把重复缓存和历史截图加入 Git 历史。
