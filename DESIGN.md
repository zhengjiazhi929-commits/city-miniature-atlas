# 山河入掌 · 视觉契约 · 0.0.2

以暖纸色界面承托有实体体积的城市微缩模型。默认桌面视角应能读出山水、主路、街区和景点之间的关系，细化围绕这一观看尺度展开。

## 界面语言

| 用途 | 当前基础色 |
| --- | --- |
| 纸色背景 | `#efede5` |
| 正文墨色 | `#233d35` |
| 主要操作 | `#245849` |
| 少量强调与键盘焦点 | `#a04931` |
| 辅助文字 | `#62695d` |
| 分隔线 | `#cdd2c5` |
| 浅色面板 | `#faf9f4` |

这些色值来自 `style.css`，属于界面样式；三维材质经光照和色调映射后的屏幕颜色不等于其源色值。地点标题使用本地 Noto Serif SC 子集并回退至宋体；正文和操作使用设备无衬线字体。维持清楚的地点层级、轻分隔线及小圆角。

桌面采用顶栏、左侧旅行索引和右侧大幅三维场景。面包屑明确当前尺度；旋转、标注和视图恢复是轻量工具。索引独立滚动，场景保持主要视觉面积。窄屏样式的存在不代表已完成移动设备验收。

## 区域与默认视角

省份和城市按照来源边界独立呈现，外围留白。浙江使用同源城市分区并集作为省级展示轮廓，避免外围大片行政海域底板；保留源岛屿和内陆水系。分区抬升是选中提示，不改变真实边界。标签可以避让并以引线连接原锚点，不能为了排版移动地理坐标。

杭州保留完整市域，初始镜头聚焦主城，拉远可看全域。使用统一基础投影，不叠加横向放大镜或拉弯湖岸和道路。杭州地形当前采用 8 倍显示高差；这是造型参数，不是实测模型比例。底座保持统一厚度，上方岩土切面随地形变化。

## 城市体量与配色

- **街区有秩序。** 住宅以成组板楼、公寓和庭院形成连续街坊；商业区以少量较高楼体、裙房形成重点；工业体量较低且宽。不同街区逐渐过渡，避免满城同高细塔或独立碎点。
- **保持远景可读。** 通用模型的宽、深、高和密度按当前观看尺度组织；不能仅把镜头拉近、放大基座或牺牲道路形状来制造体积。景点代表模型适度突出，细部主要留在独立模型中。
- **0.0.2 高低节奏与颜色成组。** 同一街区配色接近，相邻组团有明暗差。住宅以灰白、浅灰为主，商业加入银灰与少量低饱和灰蓝；避免整城过蓝、近黑塔楼或逐栋随机涂色。
- **区分表面材料。** 墙体、玻璃和金属使用独立材质分组。玻璃依靠反射环境和低粗糙度呈现幕墙感；当前环境是程序化光照，不是真实周围建筑的逐物反射。整体实例染色不能破坏玻璃与墙体的区别。
- **地面承托楼体。** 城区地面采用较深、稍暖的中性色，与较冷较亮的建筑区分；自然绿地按来源分类，加入草地、灌木及树群形成远景也可辨的绿面。机场保留其硬质设施表达，不按普通深色城区处理。
- **空间关系优先。** 主路、桥梁、树木、岸壁和楼体有几何厚度。桥梁依据源标签，普通道路不能全部架空。完整模型求地表支撑并避让水、路和邻居；不容许悬空基座、穿地或明显互穿。

通用资产在 [City Kit](assets/city-kit/README.md) 中统一维护。当地地理定位和尺度配置与资产分离；不能把某座城市的专用摆放方案当作通用模型的一部分。

## 景点表达

总览使用清楚、克制的名称和定位提示，不给机场额外特殊的视觉权重。杭州具体展示名单以 [产品契约](PRODUCT.md) 和运行时目录为准；普通标志建筑可以出现在天际线中，不必全部增加标签。

独立寺庙保留主要殿堂和必要院落，去掉无关大片场地；塔突出主体结构；古街古镇保留沿街或院落组织。西湖、千岛湖、西溪和天目山分别强调湖形与堤岛、水库群岛、湿地水网、山林路径等区别。范围依照主体和来源组织，不将自然景点统一处理成泛化山水树。

截图和 GLB 必须来自实际场景。图片概念稿、静态导出和交互网页分别说明，不能互相代替完成证明。

## 交互与检查

旋转和缩放保持可控距离，避免进入未经细化的微小道路与建筑内部。尊重系统减少动态效果设置；抬升和镜头移动不应妨碍选择。保留可见键盘焦点、来源署名及明确加载错误，不能用空白或替代地形伪装加载完成。

验收查看实际默认远景、局部街区和完整市域，分别检查城市辨识度、材质层次、接地、道路关系及导航返回。数值测试与人工画面检查并用；其他城市需要独立验证，不以杭州效果证明全站一致。

实现参数以 `style.css`、`regions.css`、`src/hangzhou-sandtable.js`、`src/hangzhou-fabric.js`、`src/city-assets.js` 及对应来源数据为准。[当前检查记录](docs/QA.md) · [地理来源与许可](NOTICE.md)。
